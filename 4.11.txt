任务：
研究bqm-wx
a、做表情功能模块、逻辑、接口调用、页面结构、整体原理
b、据a研究小程序api、组件
c、该项目模块整理






知识：
1、git Bash才可以执行ssh-keygen -t rsa -C "youremail@example.com"命令
2、获取当前页信息app.getCurrentPage()  返回route，data
3、跳转到页面 编译-添加编译模式
4、数据调试 - AppData Panel
5、居中布局
	.box{
	   display: flex;
	   justify-content: center;
	   align-item: center;
	   flex-direction: column;(项目排列方式）
	}
	.child{
	   flex: auto | flex-grow flex-shrink flex-basic
	}
6、wx接口调用是异步的，无阻塞
7、e.target 触发事件的对象 和e.currentTaret事件绑定的元素==this
8、js解析变量key  {[变量]：value}
