4.10

微信小程序

入门
代码构成：
   json配置
	app.json 全局（页面）配置（页面路径、界面表现、网络超时、底部 tab 等)
	project.config.json 开发者工具配置
	page.json 子页面配置
   WXML模板
	类似html
	组件新标签：view、button、text
	MVVM模板语法 、 wx-指令
   WXSS样式
	新尺寸单位rpx，屏宽750rpx
	app.wxss-全局样式，page.wxss-局部样式
	样式导入 @import "相对路径";
	手机宽带100vm,高度100vh
   js
小程序能力：
   	首页：pages配置的第一个页面
   	app.js：实例执行onLanuch回调
	Page构造器，onLoad回调
	组件（map及其属性）
	api（getLocation、scanCode）
发布准备：
	用户身份-权限管理
	预览
	上传代码
	小程序版本

框架
	响应的数据绑定（view、App Service）
	页面管理（路由、页面生命周期、数据、函数注册）	
	基础组件
	原生api

组件
	基础组件（标签-视图层）
	属性类型（值）
	共同属性（名）
	特殊属性（自定义）
	组件列表：View Container, Basic Content, Form, Navigation, Media, Map, Canvas

api	
	wx.on：callback 		其他：OBJECT(可定义success、fail、complete)
	API列表	
		网络 request、uploadFile、downloadFile、**socket**
		媒体 
		文件
		数据缓存(本地10M) storage（get/set/clear）（异步/sync）维度隔离、设备隔离
		位置
		设备
		界面
		WXML节点
		开放接口	
	
框架
	文件结构
	配置 app.js(pages,window,tabBar,networkTimeout,debug)
	逻辑层App Service(App,Page)
		注册程序 - App(object)  object-onLaunch,onShow,onHide,globalData(自定义)。。。   getApp()获取实例
		注册页面（Page，初始化json数据，生命周期函数）
		路由（页面栈，路由方式）
		模块化（文件作用域、模块化）
		api








hbuilder主题设置：工具-选项-HBuilder-主题
window10修改系统属性：此电脑-计算机-系统属性-搜索高级系统设置
node 集成npm包管理工具
微信小程序断点调试：打开调试器，关闭编辑器，进入source-top
断点调试中let、const定义的变量有作用于，不能访问
ESlint 静态代码检查工具
git命令 push/pull/remote -v
cat .html查看


